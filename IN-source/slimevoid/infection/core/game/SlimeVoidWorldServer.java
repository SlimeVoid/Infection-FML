package slimevoid.infection.core.game;
/*package net.minecraft.slimevoid;

import net.minecraft.server.MinecraftServer;
import net.minecraft.src.ISaveHandler;
import net.minecraft.src.WorldProvider;
import net.minecraft.src.WorldServer;
import net.minecraft.src.WorldSettings;


public class SlimeVoidWorldServer extends WorldServer{

	public SlimeVoidWorldServer(MinecraftServer minecraftserver,
			ISaveHandler isavehandler, String s, WorldProvider worldProvider,
			WorldSettings worldsettings) {
		super(minecraftserver, isavehandler, s, worldProvider, worldsettings);
	}
	
	@Override
	protected void generateSpawnPoint() {
		worldInfo.setSpawnPosition(0, 6, -20);
        worldChunkLoadOverride = false;
	}

}*/

// TODO WTF IS ALL THAT SHIT ?